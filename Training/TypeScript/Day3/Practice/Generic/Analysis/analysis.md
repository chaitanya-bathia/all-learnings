Generic is used to create a reusable component.
Generic type variables are created using <T> notation.
User can then take this generic type variable and apply their desired type to it, and use it in whatever way they want.